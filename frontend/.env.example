# Copy to .env before running locally or set as Vercel environment variables
# VITE_BACKEND_URL: your Render.com backend URL (no trailing slash)
# VITE_API_KEY: must match API_KEY set in backend/.env and Render env vars

VITE_BACKEND_URL=https://skylark-bi-agent.onrender.com
VITE_API_KEY=your-api-key-here
